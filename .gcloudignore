# Uploaded to Cloud Build for `gcloud run deploy --source .`.
# The Dockerfile runs `npm install` + `npm run build` itself, so exclude
# anything that would only bloat the upload.
.git
node_modules
dist
*.log
.env.local
